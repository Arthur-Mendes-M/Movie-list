import { Styled_AbsoluteContainer } from "./styles"

export const PopupContainer = ({children}) => {
    return (
        <Styled_AbsoluteContainer>
            {children}
        </Styled_AbsoluteContainer>
    )
}