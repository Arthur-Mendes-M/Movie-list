import { styled } from "styled-components";
import { flexBox } from "../../GlobalStyles/variables/mixins";

export const RowCardList = styled.div`
    ${flexBox(undefined, undefined, undefined, "wrap", 1)}
`